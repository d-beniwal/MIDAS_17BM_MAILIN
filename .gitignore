# Raw/derived beamline data (huge; lives on this workstation, not in git)
beamline_data/

# Old scratch work / prior draft pipeline
archive/

# Claude Code project context and docs (kept local per project convention)
.context/
CLAUDE.md
.claude/

# Pipeline outputs (calibration.json, residual_corr.bin, integrated profiles, plots)
# residual_corr.bin alone runs tens of MB per calibration -- never commit these.
pipeline_output/

# Local dev/exploratory scratch work (one-off beamline test/comparison code
# + its data, e.g. gsas2_vs_midas/) -- not part of the maintained deliverable
scratch/

# Jupyter / Python
.ipynb_checkpoints/
__pycache__/
*.pyc
.DS_Store
